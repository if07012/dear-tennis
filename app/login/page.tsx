import { LoginPage } from '@/components/login/LoginPage';

export const metadata = {
  title: 'Login — Dear Tennis',
  description: 'Sign in to your Dear Tennis member dashboard.',
};

export default function LoginRoute() {
  return <LoginPage />;
}
